# Optional modes

Coming in a later version: link recovery (putting link addresses back from the PDF itself, since Google's export keeps only the link text) and layout recovery (rebuilding tables whose cells the export scrambled). Both will be opt-in, and both will be checked like everything else.

Until then: name every lost link and every scrambled spot in the report (SKILL.md Step 6), and do not reorder anything.
